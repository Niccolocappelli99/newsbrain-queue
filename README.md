# NewsBrain public queue

This repository publishes only the sanitized job queue used by the owner's
local NewsBrain workflow. It contains no application database, credentials,
local file paths, imported reports, or Second Brain content.

